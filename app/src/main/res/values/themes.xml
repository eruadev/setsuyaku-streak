<resources xmlns:tools="http://schemas.android.com/tools">
    <style name="Theme.SkipMoney.Splash" parent="Theme.SplashScreen">
        <item name="windowSplashScreenBackground">@color/splash_background</item>
        <item name="windowSplashScreenAnimatedIcon">@drawable/splash_pig_icon</item>
        <item name="postSplashScreenTheme">@style/Theme.SkipMoney</item>
    </style>

    <style name="Theme.SkipMoney" parent="Theme.Material3.DayNight.NoActionBar">
        <item name="android:statusBarColor" tools:targetApi="l">@android:color/transparent</item>
        <item name="android:navigationBarColor">@android:color/transparent</item>
    </style>
</resources>
